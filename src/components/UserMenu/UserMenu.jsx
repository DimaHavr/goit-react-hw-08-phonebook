import { Button, Container, Text, LogOutIcon } from './UserMenu.styled';

const UserMenu = () => {
  return (
    <Container>
      <Text>mango</Text>
      <Button>
        <LogOutIcon />
      </Button>
    </Container>
  );
};

export default UserMenu;
